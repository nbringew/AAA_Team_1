# Intelligent Charging with Reinforcement Learning

## Problem Setup

The final analytical task considers an electric taxi that arrives at its home charger at
14:00 and departs at 16:00. During this two-hour window, a charging decision is made every 15
minutes, resulting in eight decisions per episode. The objective is to minimize charging cost
while leaving enough energy for the next shift. Any energy shortage at departure incurs a
penalty of EUR 100 per missing kWh, reflecting the high operational cost of an undercharged
vehicle.

To represent a heterogeneous fleet, battery capacity is sampled uniformly from 40, 60, and 77
kWh in each episode. Initial state of charge (SOC) is independently sampled between 20% and
60% of capacity. The energy required for the next shift follows
$d\sim\mathcal{N}(30,5^2)$ kWh and is clipped to the vehicle's capacity. Importantly, its
realization is revealed only at departure. The agent therefore knows the demand distribution,
but must hedge against an uncertain daily requirement while charging.

## Markov Decision Process

The charging problem is modeled as a finite-horizon Markov Decision Process. For the
price-unaware agent, the state is

$$
s_t=[SOC_t,t,C],
$$

where $SOC_t$ is the current energy level, $t\in\{0,\ldots,8\}$ is the time step, and $C$ is
battery capacity. The realized demand is deliberately excluded from the state. The
price-aware environment adds the current day-ahead price and the mean remaining day-ahead
price:

$$
s_t^{EPEX}=[SOC_t,t,C,\pi_t^{DA},\bar{\pi}_{>t}^{DA}].
$$

Four discrete actions represent charging powers of 0, 5, 11, and 22 kW. For a 15-minute
interval, the transition is

$$
SOC_{t+1}=\min(C,SOC_t+p_t\cdot0.25).
$$

The step reward is the negative charging cost. After the eighth decision, a terminal penalty
is added if $SOC_8<d$:

$$
r_8=-\text{cost}_8-100\max(0,d-SOC_8).
$$

This creates a cost--reliability trade-off: additional energy is inexpensive relative to a
shortage, but the agent must learn the tail risk from sampled experience.

## Cost Environments and Electricity Prices

Two `gymnasium` environments share the same battery dynamics and hidden-demand process. The
stylized environment applies a gently increasing tariff from EUR 0.140 to EUR 0.160 per kWh.
The EPEX environment uses 90 daily price profiles from the German--Luxembourg bidding zone for
January through March 2025. Hourly EPEX SPOT day-ahead prices are converted from EUR/MWh to
EUR/kWh and linearly interpolated to the eight quarter-hour decisions between 14:00 and 16:00.
The resulting day-ahead prices range from EUR -0.026 to EUR 0.371 per kWh, with a median of EUR
0.110 per kWh.

The day-ahead profile is observable to the price-aware agent. The price billed in the
simulation is modeled as

$$
\pi_t=\pi_t^{DA}+\varepsilon_t,
\qquad \varepsilon_t\sim\mathcal{N}(0,0.015^2)\ \text{EUR/kWh},
$$

and is not observable before the decision. The deviation represents short-term procurement
uncertainty; it is simulated rather than obtained as a second market-price dataset. Price
profiles are loaded from a local cache, with the Fraunhofer ISE Energy-Charts API used only as
a fallback.

Both environments calculate cost as price multiplied by the energy actually added. This is a
deliberate deviation from the assignment's quadratic $p^2$ charging-cost specification. The
implemented model treats energy procurement as the relevant marginal cost and does not impose
an additional power-dependent grid charge for a single vehicle with at most 22 kW. Results
therefore answer a dynamic energy-price problem and should not be interpreted as evidence for
settings in which high charging power itself is costly.

## Learning Algorithm and Benchmarks

Two Deep Q-Networks (DQN) are trained: a price-unaware agent in the stylized environment and a
price-aware agent in the EPEX environment. Both use a multilayer perceptron with two hidden
layers of 128 units and identical hyperparameters. Training lasts 200,000 time steps, equivalent
to approximately 25,000 simulated charging days. Performance is evaluated on 1,000 paired
episodes, using identical seeds so that capacity, initial SOC, demand, price day, and simulated
price deviations match across strategies.

The operational benchmark is a price-blind greedy heuristic. Since demand is unknown, it does
not charge toward the realized requirement. Instead, it targets

$$
\tau=\min(C,\mu+2\sigma)=\min(C,40\text{ kWh}),
$$

which corresponds to an approximate 97.7% service level under the assumed normal distribution.
At each step, it spreads the energy still missing from this target over the remaining time and
selects the smallest sufficient charging power.

An exact brute-force benchmark evaluates all $4^8=65{,}536$ action sequences on a subset of 100
episodes. This benchmark is an oracle: it knows the realized demand and, in the EPEX setting,
the realized billed prices in advance. It is therefore a theoretical upper bound rather than
an implementable policy.

## Results

In the stylized environment, the greedy heuristic succeeds in 98.9% of the 1,000 episodes and
achieves a mean reward of -4.548. The price-unaware DQN succeeds in only 70.1% and obtains a
mean reward of -105.426. The paired reward difference is -100.878 with a 95% confidence
interval of [-114.107, -87.649]. On the 100-episode oracle subset, mean reward is -5.599 for
greedy and -1.449 for the oracle. Greedy is therefore not exactly optimal, but it is much more
reliable than the learned policy. With little systematic price variation, the DQN learns to
charge near mean demand and underestimates the rare but severe shortage risk, whereas the
explicit $\mu+2\sigma$ rule provides an effective safety margin.

The comparison under EPEX prices leads to the same central reliability concern. Greedy again
succeeds in 98.9% of episodes, compared with 70.1% for the DQN trained without price data and
75.9% for the price-aware DQN. Their unconditional mean charging costs are EUR 1.704, EUR
1.022, and EUR 0.972 per day, respectively. These raw costs cannot be interpreted alone:
failed DQN episodes are cheaper partly because the agents purchase insufficient energy, and
their shortage penalties consequently produce much worse mean rewards.

To isolate charging-time savings, costs are compared only in the 759 episodes in which both
greedy and the price-aware DQN cover demand. Within this subset, the price-aware DQN saves EUR
0.662 per day, or 43.1%, relative to greedy and is cheaper in 89% of episodes. It also improves
on the price-unaware DQN in both success rate and mean cost. Policy maps show the intended
behavior: when SOC is sufficiently high, the price-aware agent charges at high power during
cheap intervals and waits during expensive ones; when SOC is low or departure approaches,
charging urgency increasingly dominates price.

The cost advantage nevertheless remains inseparable from inadequate demand insurance. A
success rate of 75.9% is not operationally acceptable for a taxi fleet, even when costs are
lower on successful days. The oracle reaches 100% success at approximately EUR 0.974 per day,
but it benefits from information unavailable to any real agent. The relevant conclusion is
therefore that day-ahead information has value for charge timing, while the present DQN still
requires an explicit reliability safeguard.

## Operational Implications and Limitations

Under nearly flat tariffs, the transparent service-level heuristic is preferable to DQN. Under
dynamic prices, learned control can exploit cheap intervals, particularly when the vehicle
arrives with sufficient SOC and prices vary substantially within the charging window. A
practical implementation should place the learned policy behind a safety layer, for example a
hard minimum-SOC rule during the final intervals, or train it with a stronger asymmetric
shortage objective. Private charging combined with a dynamic electricity contract is the most
controllable setting for realizing such savings; public charging with fixed tariffs offers
less opportunity for price-aware scheduling.

The analysis remains a proof of concept. It considers one two-hour charging window, stylized
battery dynamics, no charging losses or battery degradation, and a fixed normal demand
distribution. A true fixed-power flat-rate baseline has not yet been evaluated, and the
required sensitivity analysis for alternative demand levels such as 25, 30, and 35 kWh is
still missing. Moreover, the linear energy-cost model should either be complemented by the
assignment's quadratic power-cost formulation or retained only with the stated interpretation.
These additions are necessary before making a final infrastructure or deployment recommendation.
